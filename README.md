# StringCollectionFormatter

C# .Net Core 5.0 class for formatting a collection of strings.  The Format Items method for each individual item in the collection:

- Removes duplicate characters
- Removes '_' and '4' characters
- Replaces '$' with '£'
- Truncates to max length of 15 characters
